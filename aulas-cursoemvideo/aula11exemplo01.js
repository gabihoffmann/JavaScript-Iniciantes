console.log(`O console esta funcionando...`);

/*
        Condições em JavaScript

    Condição simples: (IF)
    A condicional if  é uma estrutura condicional 
    que executa a afirmação, dentro do bloco, 
    se determinada condição for verdadeira.

    Estrutura: 
    if (condition) { 
        action 
    }

*/

//Exemplo condição simples
var vel = 60.5;

if (vel > 60){
    console.log(`A velocidade ${vel} está ${vel - 60} km/h acima do permitido!`)
}
